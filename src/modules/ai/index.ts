import { moduleMeta } from '../../core/define.js';

export default moduleMeta({
  name: 'ai',
  description: 'guiBot talks back: /ask and @mention replies everywhere except channels you turn off.',
});
