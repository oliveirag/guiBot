import { moduleMeta } from '../../core/define.js';

export default moduleMeta({
  name: 'mod',
  description: 'Moderation: bans, kicks, timeouts, warns with escalation, notes, cases, purge, and channel locks.',
});
