import { moduleMeta } from '../../core/define.js';

export default moduleMeta({
  name: 'suggestions',
  description: 'Suggestions with votes, discussion threads, and approve, deny, or consider.',
});
