import { moduleMeta } from '../../core/define.js';

export default moduleMeta({
  name: 'logs',
  description: 'Logs message edits and deletes, joins and leaves, role changes, voice, and mod cases.',
});
