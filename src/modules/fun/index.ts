import { moduleMeta } from '../../core/define.js';

export default moduleMeta({
  name: 'fun',
  description: '8ball, dice, coin flips, cats, dogs, memes, ship and rate, plus trivia and tic-tac-toe.',
});
